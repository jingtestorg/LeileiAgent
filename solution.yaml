apiVersion: solution.sap/v1
kind: Solution

metadata:
  name: weather-greeting-agent-f37a3
  version: "1.0.0"
  description: "AI agent that greets users with personalized hellos based on their location and current weather conditions"

assets:
  - ref: ./assets/weather-greeting-agent/asset.yaml
